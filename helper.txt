docker compose up -d        # start in background
docker compose logs -f      # follow logs
docker compose run --rm bot post # single post
docker compose run --rm bot post --retry-failed # retry one failed item manually

docker compose run --rm bot test  # Dry-run test
docker compose run --rm bot test-vision
docker compose run --rm bot status
docker compose run --rm bot reset-history
docker compose run --rm bot reset-captions
docker compose run --rm bot reset-queue
docker compose run --rm bot reset-failed
mkdir -p ~/backups

Queue behavior:
- scraped_queue.json stores screenshots already scraped from Steam.
- failed_queue.json stores screenshots that failed during publish.
- normal post runs consume from scraped_queue first.
- if publish fails, the item moves into failed_queue for manual retry.
