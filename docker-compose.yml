services:
  bot:
    build: .
    container_name: steam-insta-bot
    restart: unless-stopped
    env_file:
      - ./enhanced_steam_bot/.env
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
