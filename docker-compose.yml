services:
  bot:
    build: .
    container_name: steam-insta-bot
    restart: unless-stopped
    env_file:
      - ./enhanced_steam_bot/.env
    ports:
      - "${PORT:-3000}:${PORT:-3000}"
    volumes:
      - ./data:/app/data
